version: '3.8'
services:
  mysql:
    image: mysql
    environment:
      MYSQL_DATABASE: test
      MYSQL_ROOT_PASSWORD: test
    ports:
      - "3306:3306"
    volumes:
      - data:/var/lib/mysql
volumes:
  data: ~
